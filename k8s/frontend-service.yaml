apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: platform
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
    type: NodePort
